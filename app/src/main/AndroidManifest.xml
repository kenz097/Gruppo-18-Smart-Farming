<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smartfarming">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Stoccaggio" android:label="@string/activityStoccaggio"></activity>
        <activity android:name=".Homepage" android:label="@string/activityHomepage"></activity>
        <activity android:name=".Raccolta" android:label="@string/activityRaccolta">

        </activity>
        <activity android:name=".Registrazione" android:label="@string/activityRegistrazione"></activity>
    </application>

</manifest>